# requirements-docker.txt
# Docker-specific dependencies — CPU-only, headless, no dev tools
# Install with: pip install -r requirements-docker.txt

# Core numerics
numpy==2.4.6
pandas==2.2.3
scipy==1.15.1

# Computer Vision — headless (no Qt/GUI dependencies)
opencv-python-headless==4.13.0.92

# MediaPipe face mesh
mediapipe==0.10.35

# Image processing
pillow==12.2.0

# ML utilities
scikit-learn==1.6.1
joblib==1.4.2

# XGBoost (residual fallback baseline)
xgboost==2.1.3

# ONNX Runtime (lightweight CPU inference, ~15MB instead of 1.5GB PyTorch)
onnxruntime==1.20.1
